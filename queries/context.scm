; context.scm — Helix breadcrumb navigation for tree-sitter-lean
;
; Shows the enclosing context (namespace / definition / etc.) in the editor
; status line, helping orient the user within deeply nested Lean files.

(definition) @context

(declaration) @context

(structure) @context

(inductive) @context

(class_inductive) @context

(initialize) @context

(namespace) @context

(section) @context
